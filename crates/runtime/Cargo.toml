[package]
name = "vulkanize-runtime"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
vulkanize-gguf = { path = "../gguf" }
vulkanize-vulkan-backend = { path = "../vulkan-backend" }
