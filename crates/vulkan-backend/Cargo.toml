[package]
name = "vulkanize-vulkan-backend"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
ash = "0.37"

[dev-dependencies]
ash = "0.37"
vulkanize-runtime = { path = "../runtime" }
